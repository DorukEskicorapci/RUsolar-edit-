#ifndef CAN_PARAMS_H
#define CAN_PARAMS_H

#include "can.h"

#define sizeofarr(x) = sizeof(x)/sizeof(x[0]);

typedef struct {
	can_param_t speed;
} can_params_t;

extern can_params_t params;



#endif
